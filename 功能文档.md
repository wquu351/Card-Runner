# 卡牌跑者 (Card Runner) - 功能文档

> Godot 4.x 项目 · GDScript · 2D竖屏跑酷 + Roguelite卡牌构建

---

## 一、项目概览

### 玩法一句话
控制角色在三车道间移动躲避下落障碍物，通过选卡获得能力叠加，活得越久分数越高。

### 技术栈
- **引擎**：Godot 4.x
- **语言**：GDScript
- **分辨率**：720 × 1280（竖屏）
- **场景根节点**：`Node2D`（main.tscn）

### 启动流程
```
游戏启动 → main.gd._ready()
    → 实例化 StartScreen（CanvasLayer, layer=200）
    → get_tree().paused = true（暂停所有游戏逻辑）
    → 显示标题"卡牌跑者" + 开始按钮 + 卡牌百科 + 最高分
    → 点击开始 / Enter / 空格 → 销毁StartScreen → paused = false → 正常游戏
```

### 控制方式
| 按键 | 功能 |
|------|------|
| A / ← | 左移 |
| D / → | 右移 |
| Q | 替身假人（主动技能） |
| E | 时间沙漏（主动技能） |
| R | 换位术（主动技能） |
| W（空中） | 二段跳 |
| P / ESC | 暂停 |

---

## 二、核心数值（player.gd 常量）

| 参数 | 值 | 说明 |
|------|-----|------|
| `BASE_SPEED` | 400 | 水平移动速度（像素/秒） |
| `BASE_JUMP_FORCE` | -600 | 跳跃力度 |
| `BASE_LIVES` | 2 | 初始生命值 |
| `BASE_INVINCIBLE_TIME` | 1.5 | 受击后无敌时间（秒） |
| `LANE_POSITIONS` | [240, 360, 480] | 三车道X坐标 |
| `LANE_SWITCH_DURATION` | 0.12 | 车道切换过渡时间（秒） |

**生命恢复**：生命值不满时启动计时器，基础恢复间隔 **10秒**（可通过快速愈合能力缩短），恢复1点。

---

## 三、能力系统（25个）

### 数据结构
每个能力对应一个 `.tres` 文件（`abilities/data/`），继承 `AbilityData`（`abilities/ability_data.gd`）：

```
AbilityData 资源字段：
├── ability_id: String        # 唯一标识符
├── display_name: String      # 显示名称
├── description: String       # 描述文字
├── rarity: int               # 0=普通 1=稀有 2=史诗 3=传说
├── stackable: bool           # 是否可叠加
├── max_stacks: int           # 最大叠加层数
├── effect_type: int          # 效果类型
├── target_attribute: String  # 影响的属性名
└── effect_value: float       # 效果值
```

管理器：`AbilityManager`（单例 `abilities/ability_manager.gd`），负责加载、注册、叠加、查询全部能力。

### 稀有度与权重

| 稀有度 | 权重 | 颜色 | 数量 |
|--------|------|------|------|
| 普通 (Common) | 50 | 白/灰 | 6 |
| 稀有 (Rare) | 30 | 蓝 | 11 |
| 史诗 (Epic) | 15 | 紫 | 5 |
| 传说 (Legendary) | 5 | 金 | 3 |

> 首次选卡时，稀有和史诗权重临时+50%。

### 全部能力列表

#### 1. 防御/护盾类（7个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 能量护盾 | score_shield | 稀有 | 否 | 自动获得护盾，**每5秒恢复1层**（固定最多2层）。受击消耗1层并释放冲击波（**可触发能量核心爆炸**） |
| 镜之盾 | mirror_shield | 史诗 | 否 | 头顶3段蓝色护盾条，每段挡1次伤害，**每8秒恢复1段** |
| 伙伴卫星 | satellite | 史诗 | 最多2层 | 环绕玩家的自动防御卫星（蓝色），碰撞摧毁障碍物（**触发能量核心爆炸**） |
| 幻影斗篷 | phantom_cloak | 史诗 | 否 | 每**8秒**自动虚化**3秒**，期间穿墙无敌。三阶段循环：等待触发(8s)→激活中(3s)→冷却(8s) |
| 凤凰涅槃 | phoenix_reborn | 传说 | 否 | 死亡时原地复活恢复满血+全屏冲击波清障。冷却**15秒**，可多次使用 |
| 最后一搏 | last_stand | 传说 | 否 | 致命伤害时免死并获得**20条临时生命**（显示绿色高亮"实现之魂"），耗尽后真正死亡。每局1次 |
| 触壁瞬移 | wall_phase | 稀有 | 否 | 碰到屏幕边缘从另一边出现 |

#### 2. 攻击/清障类（5个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 定时炸弹 | time_bomb | 稀有 | 否 | 每**5秒**放置炸弹，2秒后爆炸大范围清障（**触发能量核心连锁爆炸**） |
| 弹射飞镖 | ricochet_dart | 稀有 | 否 | 每**6秒**发射飞镖追踪最近障碍物，弹射最多4个。命中能量核心立即触发爆炸并停止弹射 |
| 弹射球 | yoyo | 稀有 | 否 | 每**1.5秒**自动向前发射弹射球，击碎前方障碍物（不可升级） |
| 火焰新星 | fire_nova | 稀有 | 否 | 每**6秒**释放火焰新星向外扩散清障（**波及能量核心触发爆炸**） |
| 复仇之魂 | revenge_spirit | 稀有 | 否 | 获得红色光环复仇护盾，被消耗时向上发射光柱清障。冷却**15秒** |

#### 3. 移动/位移类（3个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 二段跳 | charged_jump | 稀有 | 否 | 空中按W再跳一次 |
| 换位术 | swap | 传说 | 否 | 与随机障碍物互换位置+全屏冲击波清障。冷却**20秒**（R键） |
| 轻盈闪避 | auto_dodge | 稀有 | 否 | 障碍物即将落地时自动侧移躲避，0.2秒无敌。冷却**5秒** |

#### 4. 主动技能类（2个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 替身假人 | decoy | 稀有 | 否 | 创造假人吸收周围障碍物3秒，角色无敌3秒。冷却**10秒**（Q键） |
| 时间沙漏 | time_slow | 史诗 | 否 | 减速所有障碍物3秒。冷却**12秒**（E键） |

#### 5. 碰撞体/体积类（2个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 快快变小 | soft_kneepads | 普通 | 最多5层 | 碰撞体缩小至0.8倍/层 |
| 小小小plus | lucky_rabbit_foot | 稀有 | 否 | 碰撞体缩小30%，首个能力额外缩小15% |

#### 6. 得分/经济类（3个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 得分锦囊 | score_pouch | 普通 | 最多5层 | 分数倍率 **+30%/层**（effect_value=1.30，乘法叠加） |
| 磁铁手套 | magnet_gloves | 普通 | 否 | 吸引附近金币 |
| 金币加成 | coin_bonus | 普通 | 否 | 每收集一个金币，全局分数倍率永久**+0.02** |

#### 7. 生命类（2个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 备用生命 | spare_life | 普通 | 最多5层 | 最大生命 +1/层 |
| 快速愈合 | fast_recovery | 普通 | 最多3层 | 加快生命恢复速度 |

#### 8. 挑战/加成类（1个）

| 名称 | ID | 稀有度 | 叠加 | 效果详情 |
|------|-----|--------|------|----------|
| 无伤挑战者 | flawless_challenge | 史诗 | 否 | 8秒不受伤获得得分**+150%**加成（无速度加成） |

---

## 四、计分系统（game_manager.gd）

### 分数增长机制
- 每 **0.1秒** 计算一次分数
- 公式：`gained = int(2.0 × dynamic_mult × ability_mult)`
- `dynamic_mult`：基于当前分数动态倍率（最高6x，每500分递增）
- `ability_mult`：来自能力系统的倍率加成（得分锦囊、金币加成、无伤挑战者等）

### 最高分系统
- 存档路径：`user://fish_game_save.cfg`
- 游戏结束时自动比较并保存
- 启动界面和结束弹窗均显示历史最高分
- 打破纪录时标注"新纪录!"

---

## 五、选卡系统

### 触发阈值（upgrade_trigger.gd）

| 次序 | 分数阈值 |
|------|---------|
| 第1次 | 100 |
| 第2次 | 250 |
| 第3次 | 400 |
| 第4次 | 600 |
| 第5次 | 800 |
| 第6次起 | 每+1000分触发一次（1800, 2800, 3800...） |

### 选卡流程
```
分数达到阈值 → upgrade_triggered信号
    → GameFlowManager.request_card_selection()
    → 从能力池随机抽3张卡（首次选卡稀有/史诗权重+50%）
    → 弹出选卡UI（card_selection_ui.tscn）
    → 玩家点击一张卡
    → complete_card_selection() → AbilityManager.add_ability()
    → 游戏继续
```

### 能力池耗尽
- 所有能力达到最大层数后不再弹窗
- 改为给予3秒无敌 + 清屏
- 进度条变灰显示"已空"

---

## 六、障碍物系统

### 类型及概率（obstacle_probability.gd）

| 类型 | 权重 | 概率 | 特殊规则 |
|------|------|------|----------|
| 普通障碍物 | 58 | 58% | 无 |
| 能量核心 | 35 | 35% | 无 |
| 大块头 | 7 | 7% | 冷却2.5秒，连续≤3个 |

### 能量核心（energy_core.gd）
- 外观：亮红色圆形（直径55px），带电火花粒子
- 被以下技能摧毁时触发**爆炸**（光圈半径120px，清除同轨道所有障碍物）：
  - 弹射飞镖、火焰新星、伙伴卫星、定时炸弹、能量护盾冲击波
- 普通护盾抵挡不算摧毁，不触发爆炸

### 大块头（big_block.gd）
- 两倍大的蓝色方块，横跨两轨道
- 只在左轨(0)或中轨(1)生成
- 下落速度与普通障碍物一致

### 波浪阵模式（wave_manager.gd）
- 轨道序列：左→中→右→中→左... 循环 [0,1,2,1]
- 可混合所有障碍物类型

### 难度系统
- 随时间增长，影响速度和生成频率
- 上限：最大难度8.0，最大频率3.0次/秒
- 连续3局存活<30秒，下一局初始难度-15%

---

## 七、护盾优先级（受击时依次检查）

```
1. 能量护盾（能量满时触发，冲击波可引爆能量核心）
2. 镜之盾（3段护盾条）
3. 复仇之魂护盾（触发光柱清障）
4. 生命值扣减
5. 凤凰涅槃（死亡时复活）
6. 最后一搏（致命免死，20条临时生命，每局1次）
```

---

## 八、UI系统

### 界面布局

```
┌─────────────────────────────┐
│ Score: 0 (×1.00)   ❤ 1/1  │  ← 左上分数(金色40px) + 右上生命(红36px)
│                             │
│[选卡│                      │  ← 左侧竖向进度条(18px宽)
│ 进  │  [能力图标列表]      │  ← 能力HUD（进度条右侧）
│ 度  │                      │
│ 条  │                      │
│     │                      │
│     │              [技能栏] │  ← 底部居中6个技能卡片+冷却条
│     │         [玩家角色]   │
└─────────────────────────────┘
```

### 各UI组件

| 组件 | 文件 | 位置 | 说明 |
|------|------|------|------|
| ScoreLabel | main.tscn内建 | 左上 | 当前分数+倍率，金色40px |
| LivesLabel | main.tscn内建 | 右上 | ❤ 当前/最大，最后一搏时绿色高亮 |
| RecoveryLabel | main.tscn内建 | 生命下方 | "恢复 Xs"，浅红20px，不满血时显示 |
| CardProgressUI | ui/card_progress.gd | 左侧竖条 | 18px宽，白→蓝→金渐变，空池变灰 |
| AbilityHUD | ui/ability_hud.gd | 左侧 | 已获能力图标+层数，颜色按稀有度 |
| SkillBar | ui/skill_bar.gd | 底部居中 | 6张技能卡片（缩写+按键+冷却条） |
| PassiveSkillBar | ui/passive_skill_bar.gd | 右侧 | 被动技能列表（名称+状态+冷却进度条） |
| PauseMenu | ui/pause/pause_menu.tscn | 全屏 | P/ESC暂停 |
| StartScreen | ui/start_screen/ | 全屏 | 启动界面（见第九节） |

### 被动技能栏冷却显示（passive_skill_bar.gd）
右侧垂直面板，按获取顺序显示被动技能，每个含：
- 图标 + 名称（截断5字）+ "自动"状态标签
- 底部冷却进度条

支持冷却显示的能力：ricochet_dart、time_bomb、auto_dodge、fire_nova、revenge_spirit、phoenix_reborn（三阶段）、yoyo、score_shield（层数比）

---

## 九、启动界面（StartScreen）

### 文件
- 脚本：`ui/start_screen/start_screen.gd`
- 场景：`ui/start_screen/start_screen.tscn`
- 通过 CanvasLayer(layer=200) 实例化到 main.gd，确保UI布局正常

### 界面组成
```
┌─────────────────────────┐
│                         │
│       卡牌跑者           │  ← 标题（呼吸动画，金色44px）
│                         │
│     ▶ 开始游戏           │  ← 按钮（绿色26px）/ Enter / 空格
│     卡牌百科             │  ← 按钮（蓝色18px）
│                         │
│    最高分: XXXX          │  ← 从存档读取
│  按 Enter / 空格 开始    │  ← 提示文字
│                         │
└─────────────────────────┘
```

### 卡牌百科
点击"卡牌百科"打开全屏深色面板：
- 隐藏主界面，显示全屏滚动列表
- 按**分类**展示25张卡牌（左侧色条+名称+稀有度标签+描述+叠加信息）
- **关闭**按钮 / **ESC** / 点击外部区域均可关闭
- 关闭后恢复主界面

### 关键技术点
- `process_mode = PROCESS_MODE_ALWAYS`：确保在 `get_tree().paused = true` 时仍能响应输入
- 主界面和百科通过 `visible` 切换，不会同时显示

---

## 十、文件结构

```
fish-game-lite/
├── main.tscn                    # 主场景（Node2D根节点）
├── main.gd                      # 主脚本：初始化背景、实例化启动界面、管理游戏状态
├── player.gd                    # 玩家：移动、跳跃、碰撞、全部能力效果实现
├── obstacle.gd                  # 普通障碍物
├── obstacle_spawner.gd          # 障碍物生成器（集成概率模块）
├── obstacle_probability.gd      # 统一概率管理（固定数组避免顺序问题）
├── game_manager.gd              # 游戏管理器：分数、生命、难度、最高分存取
├── texture_generator.gd         # 程序化纹理生成
├── light_trail.gd               # 拖尾特效（线段+移动光点）
│
├── energy_core.gd / .tscn       # 能量核心（红色圆形，爆炸链）
├── big_block.gd / .tscn         # 大块头（2倍大蓝色方块）
├── wave_manager.gd              # 波浪阵掉落模式
│
├── time_bomb.gd / .tscn         # 定时炸弹（5s间隔）
├── ricochet_dart.gd / .tscn     # 弹射飞镖（6s间隔）
├── fire_nova.gd / .tscn         # 火焰新星（6s间隔）
├── satellite.gd                 # 伙伴卫星
│
├── abilities/
│   ├── ability_data.gd          # 能力数据基类（Resource）
│   ├── ability_manager.gd       # 能力管理器（单例式）
│   └── data/                    # 25个 .tres 能力资源文件
│
├── systems/
│   ├── game_flow_manager.gd     # 游戏状态机（游玩/选卡/暂停/结束）
│   └── upgrade_trigger.gd       # 选卡触发器（基于分数阈值）
│
├── coin.gd                      # 金币（收集触发coin_bonus倍率）
├── line_2d.gd                   # 拖尾线段
│
└── ui/
    ├── ability_hud.gd           # 左侧能力图标面板
    ├── ability_stats_panel.gd   # 能力统计面板
    ├── card_progress.gd         # 选卡进度条
    ├── passive_skill_bar.gd     # 右侧被动技能栏（含冷却条）
    ├── skill_bar.gd             # 底部主动技能栏
    ├── skill_classifier.gd      # 技能分类定义（被动/主动判定）
    │
    ├── card_selection/
    │   └── card_selection_ui.gd # 选卡界面（3选1）
    │
    ├── start_screen/            # ⭐ v2.3新增
    │   ├── start_screen.gd      # 启动界面 + 卡牌百科
    │   └── start_screen.tscn    # 启动界面场景
    │
    └── pause/
        └── pause_menu.tscn      # 暂停菜单
```

---

## 十一、信号流

```
UpgradeTrigger                    GameManager                 GameFlowManager
     │                               │                            │
     ├─ upgrade_triggered ──────────►│                            │
     │                               ├─ score_gained(amount)     │
     │                               │                            │
     ├─ pool_exhausted ─────────────►│                            │
     │                               │                      pool_exhausted_bonus ──► (清屏+无敌)
     │                               │                            │
     │                          request_card_selection() ◄───────┤
     │                               │                      card_selection_started(cards)
     │                               │                            │
     │                               │                      card_selection_completed(ability)
     │                               │                            │
     │                               │                     └─► AbilityManager.add_ability()
     │                               │                              ├─ ability_added(ability, stacks)
     │                               │                              └─ ability_stack_changed(id, stacks)

AbilityManager 信号消费者：
  ability_added      → passive_skill_bar, ability_hud, ability_stats_panel
  ability_stack_changed → passive_skill_bar, ability_hud
```

---

## 十二、版本历史

### v2.3 - 启动界面 & 卡牌百科 & Bug修复
- ✨ 新增启动界面：游戏启动时显示标题画面，暂停游戏逻辑，点击/Enter/空格进入
- ✨ 新增卡牌百科面板：按分类展示25张卡牌详细信息，全屏滚动
- 🔧 main.gd重构：CanvasLayer包裹启动界面，PROCESS_MODE_ALWAYS保证暂停时可交互
- 🐛 修复 ability_stats_panel.gd 信号参数不匹配（ability_added发Object，函数期望String）
- 🐛 修复幻影斗篷UI冷却条：补充"等待触发"阶段的显示（之前始终满条）

### v2.2 - 功能更新 & Bug修复
- ❌ 删除落地缓冲能力（landing_buffer）
- ✨ 新增金币加成能力（coin_bonus）：每金币全局倍率+0.02，不可叠加
- ✨ 新增最高分系统（ConfigFile持久化）
- 🐛 修复幻影斗篷冷却条（激活时长递减+冷却进度递增）
- 🐛 修复弹射球冷却条（新增yoyo case）
- 🐛 修复被动技能栏间距（无效技能定义导致面板过大）

### v2.1 - 文档修正 & 视觉优化

### v2.0 - 障碍物系统升级 & 技能联动
- ⭐ 拖尾特效、能量核心、大块头、波浪阵、统一概率模块
- 🔧 全部攻击技能适配能量核心爆炸链
- 🔧 最后一搏UI完善（20条临时生命绿色高亮）
- 🔧 修复物理回调、概率Dictionary顺序、大块头无冷却等Bug

### v1.x - 基础版本
- 初始架构：三车道跑酷、25种能力、选卡系统、难度曲线
- 多轮能力重命名和数值调整（详见历史记录）
